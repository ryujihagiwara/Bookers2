<div class="row">

<div class="col-md-8 col-md-offset-3">

<div class="row ml">

<div class="col-md-8">

<h2>Editing Book</h2>

<% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
        <% @book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<%= form_for(@book) do |f| %>

<h4>Title</h4>
<%= f.text_field :title %>

<h4>Opinion</h4>
<%= f.text_area :body %><br>
<%= f.submit 'Update Book', class: "btn btn-primary btn-sm btn-block" %>

<% end %>

<p><%= link_to 'Show', book_path(@book.id) %>|
<%= link_to 'Back', books_path %></p>

</div>

</div>

</div>

</div>